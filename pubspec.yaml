name: age_of_scribes_gui
description: "Age of Scribes SSE Control Panel - Flutter web application for monitoring and controlling the Age of Scribes Social Simulation Engine."
publish_to: 'none'
version: 1.0.0

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  http: ^1.1.0
  fl_chart: ^0.68.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
